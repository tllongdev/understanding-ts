# Understading TypeScript

### review of the core concepts of TS

#### scripts:

- "start" runs lite-server on localhost:3000
- (in a separate terminal) "mon" runs nodemon auto compile ts to js on save
